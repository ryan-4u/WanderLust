<nav class="navbar navbar-expand-md sticky-top" data-bs-theme="light">
  <div class="container-fluid px-4">
    <a class="navbar-brand" href="/">
      <i class="fa-regular fa-compass"></i>
      <span class="navbar-brand-text">Wander<span style="color:var(--red)">Lust</span></span>
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <div class="navbar-nav me-3">
        <a class="nav-link nav-explore" href="/listings">Explore Stays</a>
      </div>

      <div class="navbar-nav mx-auto">
        <form class="search-form" action="/listings" method="GET" id="search-form">
          <input
            class="search-inp"
            type="text"
            name="q"
            id="search-input"
            value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
            placeholder="Search destinations..."
          />
          <button class="search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>
      </div>

      <div class="navbar-nav ms-auto d-flex align-items-center gap-1">
        <a class="nav-link-host" href="/listings/new">List your Home</a>
        <% if(!currUser) { %>
          <a class="nav-btn nav-btn-outline" href="/signup">Sign Up</a>
          <a class="nav-btn nav-btn-solid" href="/login">Log In</a>
          <% } %>
        <% if(currUser) { %>
          <a class="nav-btn nav-btn-favorites <%= typeof showingFavorites !== 'undefined' && showingFavorites ? 'favorites-active' : '' %>" href="/listings?favorites=true">
            <i class="<%= typeof showingFavorites !== 'undefined' && showingFavorites ? 'fa-solid' : 'fa-regular' %> fa-heart"></i> Favorites
          </a>
          <a class="nav-btn nav-btn-solid" href="/logout">Log Out</a>
        <% } %>
      </div>

    </div>
  </div>
</nav>